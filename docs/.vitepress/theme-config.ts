/**
 * VitePress 主题配置
 * 
 * 这个文件包含主题相关的配置，可以被多个地方导入使用：
 * - config.ts: VitePress 主配置
 * - 插件: 读取配置信息
 * - 工具脚本: 构建时使用
 */

/**
 * 示例文件目录配置
 * 
 * 源文件位于: docs/{EXAMPLE_DIR}/
 * 自动复制到: docs/public/{EXAMPLE_DIR}/
 * HTTP 访问路径: /{EXAMPLE_DIR}/
 * 
 * @example
 * '/example1/' - 默认配置
 * '/demos/' - 自定义示例目录
 * '/code-samples/' - 另一个示例
 */
export const EXAMPLE_DIR = '../example3/';

/**
 * 其他主题配置可以在这里添加...
 */

